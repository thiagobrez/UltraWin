# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

In any PR with user-visible changes, run `npx changeset`, pick
patch/minor/major, write a short summary, and commit the generated
`.changeset/*.md` file. See [RELEASING.md](../RELEASING.md) for the full
release flow.
